{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.10.2",
    "react-dom": "^16.9.0",
    "react-scripts": "3.2.0",
    "format-duration": "^1.3.1",
    "node-sass": "^4.13.1",
    "pluralize": "^8.0.0",
    "react-router-dom": "^5.0.1",
    "sass": "^1.23.0"
  },
  "scripts": {
    "sass:build": "sass --update src/styles/scss:src/styles/css",
    "sass:watch" : "sass --watch src/styles/scss:src/styles/css",
    "start": "yarn install && yarn sass:watch | react-scripts start",
    "build": "yarn install && yarn sass:build && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
